# 🚀 DEPLOY SEKARANG - COPY PASTE COMMAND

## Option 1: Langsung Jalankan di PowerShell/CMD (RECOMMENDED)

Copy command ini dan paste di terminal baru:

```bash
ssh tholib_server@192.168.1.27 "cd /var/www/esppd && git pull origin main && composer install --no-dev --optimize-autoloader && php artisan migrate --force && php artisan config:cache && php artisan route:cache && php artisan optimize && echo 'DEPLOYMENT COMPLETE!' && echo 'https://esppd.infiatin.cloud'"
```

**Saat diminta password**, ketik: `065820Aaaa`

---

## Option 2: Jalankan Batch File

Buka Command Prompt di folder project:

```cmd
c:\laragon\www\eSPPD_new\deploy_now.bat
```

---

## Option 3: Manual Step-by-Step

Buka terminal/PowerShell:

```bash
ssh tholib_server@192.168.1.27
```

Password: `065820Aaaa`

Lalu jalankan satu per satu:

```bash
cd /var/www/esppd
git pull origin main
composer install --no-dev --optimize-autoloader
php artisan migrate --force
php artisan config:cache && php artisan route:cache && php artisan optimize
echo "✅ DONE! Check https://esppd.infiatin.cloud"
```

---

## ✅ Verify After Deploy

Test di browser - semua harus buka tanpa error:

1. https://esppd.infiatin.cloud/admin/user-management
2. https://esppd.infiatin.cloud/admin/role-management
3. https://esppd.infiatin.cloud/admin/organization-management
4. https://esppd.infiatin.cloud/admin/delegation-management
5. https://esppd.infiatin.cloud/admin/audit-logs
6. https://esppd.infiatin.cloud/admin/activity-dashboard
7. https://esppd.infiatin.cloud/dashboard/approval-status
8. https://esppd.infiatin.cloud/dashboard/my-delegations

---

## 🔍 Check Logs Jika Ada Error

```bash
ssh tholib_server@192.168.1.27
tail -f /var/www/esppd/storage/logs/laravel.log
```

---

**Status**: ✅ READY TO DEPLOY  
**All Code**: Committed to GitHub  
**Files**: 8 pages + deployment scripts ready
