# 🚀 QUICK DEPLOYMENT - MANUAL STEPS

## SSH Deployment (Interactive)

**Paste commands satu per satu di server:**

```bash
# 1. SSH ke server
ssh tholib_server@192.168.1.27
# Password: 065820Aaaa

# 2. Di dalam server, jalankan:
cd /var/www/esppd

# 3. Git pull
git pull origin main

# 4. Composer install
composer install --no-dev --optimize-autoloader

# 5. Database migrations
php artisan migrate --force

# 6. Cache & optimize
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan optimize
php artisan cache:clear

# 7. Done! Check:
php artisan about

# 8. Exit
exit
```

---

## Verify Deployment

After deployment, test these URLs:

```
✅ https://esppd.infiatin.cloud/admin/user-management
✅ https://esppd.infiatin.cloud/admin/role-management
✅ https://esppd.infiatin.cloud/admin/organization-management
✅ https://esppd.infiatin.cloud/admin/delegation-management
✅ https://esppd.infiatin.cloud/admin/audit-logs
✅ https://esppd.infiatin.cloud/admin/activity-dashboard
✅ https://esppd.infiatin.cloud/dashboard/approval-status
✅ https://esppd.infiatin.cloud/dashboard/my-delegations
```

---

## Logs

```bash
ssh tholib_server@192.168.1.27
tail -f /var/www/esppd/storage/logs/laravel.log
```

---

## GitHub Actions Automatic Deployment

For automatic deployment on every push to `main`:

1. **In GitHub Repository**:
   - Go to Settings → Secrets and variables → Actions
   - Click "New repository secret"
   - Name: `SSH_PASSWORD`
   - Value: `065820Aaaa`

2. **Workflow file** is at `.github/workflows/deploy.yml`

3. **Next push to main** will automatically deploy

---

**Status**: ✅ Ready to Deploy
**Method**: SSH Interactive or GitHub Actions (after secret setup)
